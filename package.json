{
  "name": "wenming",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "node --import ./test-support/register-css-loader.mjs --test",
    "worker:dev": "npm run build && wrangler dev --port 8787",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "wrangler": "^4.39.0"
  },
  "dependencies": {
    "core-js": "^3.49.0",
    "html2canvas": "^1.4.1",
    "jspdf": "^4.2.1"
  }
}
